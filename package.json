{
  "name": "focused-youtube",
  "version": "3.0.0",
  "scripts": {
    "build": "mode=${npm_config_mode:-chromium} && vite build --config vite.popup.config.js --mode $mode && vite build --config vite.background.config.js --mode $mode && vite build --config vite.welcome.config.js --mode $mode && vite build --config vite.content_script.config.js --mode $mode && mkdir -p dist_$mode && rsync -av output_background_script/* dist_$mode && rsync -av output_popup/* dist_$mode && rsync -av output_welcome/* dist_$mode && rsync -av output_content_script/* dist_$mode && rm dist_$mode/index.html && node scripts/build.js --mode $mode",
    "build:all": "npm run build:chromium && npm run build:firefox && npm run zip:all",
    "build:chromium": "npm run build --mode=chromium",
    "build:firefox": "npm run build --mode=firefox",
    "zip:all": "zip -r \"focused-youtube-chromium.zip\" dist_chromium && zip -r \"focused-youtube-firefox.zip\" dist_firefox",
    "dev": "nodemon --ext js,vue,css,json,svg,png,html,ttf --watch 'src/**' --watch 'public/**' --watch 'platforms/**' --exec 'npm run build && node scripts/build.js --mode chromium'",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore --fix src",
    "format": "prettier .  --write"
  },
  "dependencies": {
    "vue": "^3.5.6"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.1.3",
    "eslint": "^9.10.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-vue": "^9.28.0",
    "nodemon": "^3.1.4",
    "sass": "^1.78.0",
    "vite": "^5.4.14",
    "vite-plugin-eslint": "^1.8.1"
  },
  "type": "module"
}
